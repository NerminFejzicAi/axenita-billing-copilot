# Reproducibility: refuse to install with a Node/pnpm version that does not match
# the versions pinned in the root package.json "engines" field (00 §4.1, §5.3).
engine-strict=true

# Keep dependency resolution strict: no phantom dependencies from hoisting.
resolution-mode=highest
auto-install-peers=false
strict-peer-dependencies=false
