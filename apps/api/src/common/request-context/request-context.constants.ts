/** Correlation header defined by 03 §3.5. Lower case, because Node normalises headers. */
export const REQUEST_ID_HEADER = 'x-request-id';

/** Canonical spelling used when writing the header back to the client. */
export const REQUEST_ID_HEADER_NAME = 'X-Request-ID';
